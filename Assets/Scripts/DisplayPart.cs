using UnityEngine;
using System.Collections;

public class DisplayPart : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
    void OnMouseDown()
    {
        //Debug.Log("ok");
        //GameObject.FindGameObjectsWithTag("Player")[0].GetComponent<MoveitMoveit>().Addpart(PartManager.getPart(name));
    }
    // Update is called once per frame
    void Update()
    {
       
    }
}
